# Server configuration
PORT=4000
NODE_ENV=development

# OpenStreetMap / Nominatim configuration
# The base URL can stay as the public nominatim instance or point to your own deployment.
OPENSTREETMAP_BASE_URL=https://nominatim.openstreetmap.org
# Please provide a descriptive user agent that includes a way to reach you.
OPENSTREETMAP_USER_AGENT=recycle-backend/1.0 (+https://your-domain.example/contact)
# Optional: supply an email address as contact information for the OpenStreetMap service.
OPENSTREETMAP_EMAIL=you@example.com
