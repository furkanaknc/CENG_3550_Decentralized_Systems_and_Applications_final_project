name: green_cycle
version: 0.1.0
publish_to: 'none'
description: Mobile client for courier-assisted recycling pickups and rewards
environment:
  sdk: '>=3.3.0 <4.0.0'
dependencies:
  flutter_dotenv: ^5.1.0
  flutter:
    sdk: flutter
  http: ^1.2.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
  assets:
    - .env
